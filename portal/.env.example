# Supabase Configuration
# Get these from your Supabase project settings > API

# Required: Supabase project URL
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"

# Required: Supabase anon/public key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# Required: Supabase service role key (server-side only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Required: JWT secret for session tokens (generate a secure random string)
JWT_SECRET="generate-a-secure-random-string-minimum-32-chars"
